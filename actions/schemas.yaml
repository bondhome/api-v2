Actions:
  example:
  - TurnOn
  - TurnOff
  type: string
Args:
  type: object
  properties:
    argument:
      type: integer
      description: |
        Optional argument for the action. Almost always an integer.
        Whether this is required depends on the specific action being executed.
      example: 4
